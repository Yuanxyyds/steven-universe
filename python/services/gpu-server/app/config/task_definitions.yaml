# Task Definitions - Pre-defined Task Templates
#
# Defines tasks that clients can invoke by name.
# Each task specifies defaults that can be overridden by the client.
#
# Purpose: Provide simple, named tasks with sensible defaults
#
# Client Usage:
#   Request by name with optional overrides:
#   {
#     "task_name": "loading-test",
#     "task_difficulty": "high",  # Override default
#     "metadata": {"custom": "value"}
#   }
#
# Structure:
#   {task_name}:
#     description: Human-readable description
#     task_type: session (oneoff removed)
#     task_difficulty: low | high (default, can be overridden)
#     model_id: Model identifier (optional, can be overridden)
#     timeout_seconds: Task execution timeout (can be overridden)
#     idle_timeout_seconds: Session idle timeout before termination
#     scheduler_type: centralized | distributed
#     metadata: Default parameters (merged with client metadata)

# =============================================================================
# Test Tasks
# =============================================================================

loading-test:
  description: "Test task that simulates GPU model loading and unloading"
  task_type: session
  task_difficulty: low
  timeout_seconds: 10
  idle_timeout_seconds: 5
  scheduler_type: centralized
  metadata: {}

download-pipeline-test:
  description: "Session-based model download pipeline test"
  task_type: session
  task_difficulty: low
  model_id: Qwen3-4B-Instruct
  timeout_seconds: 10
  idle_timeout_seconds: 30
  scheduler_type: centralized
  metadata: {}

# =============================================================================
# LLM Tasks
# =============================================================================
